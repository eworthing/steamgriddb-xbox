### Loop Counter
Loop 4 of 10 (cap)

### System Flag
[STATE: CONTINUE]

---

## Contest Verdict
**Promising, but architecturally immature.**

Concurrency is credited this loop for a real, already-landed fix: F-015 (StoreNameLookup's three unsynchronized caches) is confirmed resolved and holding in current source (commit d062d1d). This loop's own investigation found the identical defect class in a second location — `SteamGridDbClient.CapsuleParseNotes` and `FixLog`'s three static fields (F-017, new, queued) — which tempers how far concurrency moves. This loop's own pick, F-016, breaks the longest-running stall in this project's history: `test_strategy` has scored exactly 6.5 with `delta: SAME` for all 13 loops of this project's review record (10 pre-reset + 3 post-reset), and `architecture_quality` has been flat at 7.5 for 9 of those. F-016 extracts `PrimaryWidget`'s untested library-operation guard into a small, directly-testable `LibraryOperationGuard` class — following the exact Compute/Do split `TESTING.md` already documents for `GameImages`/`OperationReport`/`ManifestEntryIdentity` — and adds direct unit tests asserting the exact mutation named as an uncaught gap on every prior loop. What keeps this short of contest-grade: `PrimaryWidget.xaml.cs` still spans five concerns in one Module, `data_flow`'s ambient-state count grew from 4 to 7 confirmed instances this loop (still capped, not worsened), and F-011 remains genuinely blocked by the standing user constraint.

**Prior-audit adopt-or-falsify** (run after this loop's independent scorecard draft, per blind-critic ordering): `CODE-REVIEW.md`, `TESTING.md` and `ARTWORK-SELECTION.md` were adopted/falsified against current source in loop 1 with no open claim dropped; this loop re-confirmed no new prior-audit claim changed status. `TESTING.md` was re-read in full this loop and directly informed F-016's remedy (its own documented Compute/Do split for `PrimaryWidget.xaml.cs`'s untestable surfaces); grep of `CODE-REVIEW.md` for the guard/session-token/CapsuleParseNotes symbols this loop's findings cite returned no matches — F-016 and F-017 are novel discoveries, not pre-documented claims being adopted.

## Scorecard (1-10)

- Architecture quality: **7.5** | SAME | Direct full re-read this loop of `PrimaryWidget.xaml.cs` (2067 lines pre-fix, current) confirms the same five concerns in one Module as loops 1-3: library load/manifest parsing, bulk fix/restore/revert, grid picker, search panel, single-game ops. Triggers the Stalled-Dimension Sweep this loop (SAME for loops 1-3 of this run, and 9 of the last 10 loops overall). Candidate named and acted on: F-016 (this loop's Priority-1 pick) extracts the library-operation guard out of this file into `Services/Library/LibraryOperationGuard.cs` — a real, if modest, reduction in the file's concern count — but per this project's own established "credit lands at the start of the next loop" convention (Step 1 scores source as it exists *before* this loop's own Step 2/3 edit), this score reflects the pre-fix file and stays SAME; the improvement is credited at loop 5's Step 1 against this loop's own commit SHA.
- State management and runtime ownership: **9.5** | SAME | `residual_disposition: accepted` | Direct re-read this loop of `PrimaryWidget.xaml.cs`'s guard paths (`TryBeginLibraryOperation`/`EndLibraryOperation`/`IsLibraryOperationBlocking`, `GridImage_Click`:1531, `RestoreBackup_Click`:1916, pre-fix line numbers) re-confirms F-014's fix holds with no unguarded single-game write path. The 9-anchor is met. Residual blocking 10 unchanged: `SteamGridDbClient.CapsuleParseNotes` (`SteamGridDbClient.cs:47`) is `public static readonly List<string>` — the `readonly` only pins the reference, not the contents. **Adversarial Pass re-run this loop**: smallest fix (private backing field + `IReadOnlyList<string>` accessor) still passes SPT Q1-Q4 but fails Q5 comparatively — the gain (defense-in-depth encapsulation) is real but smaller than this loop's actual pick (F-016, which breaks a 13-loop stall on two dimensions at once), and this loop separately discovered F-017 (a genuinely distinct concurrency-safety defect in the same field's `NoteCapsuleParse` method — the encapsulation concern and the atomicity concern are different consequences of the same field, and a future fix to F-017 may naturally resolve this residual as a side effect; noted in Deepening Candidates). SPT-rejected on Q5; residual holds, accepted.
- Domain modeling: **9.5** | SAME | `residual_disposition: accepted` | Direct re-read this loop of `GameEntry.cs` (196 lines, in full) re-confirms the parallel-fields case (`HasSteamGridDBMatch`/`OfficialCapsuleUrl`/`SteamGridDbGameId`, lines 113-145) unchanged. **Adversarial Pass re-run**: the previously-proposed smallest fix (readonly-struct-with-factories) still fails SPT Q2 — `GameEntry` still implements `INotifyPropertyChanged` and is two-way XAML-data-bound; a readonly struct cannot back that binding shape without a separate wrapper layer, a larger redesign not the smallest honest fix. Residual holds, accepted.
- Data flow and dependency design: **7.5** | SAME | Triggers the Stalled-Dimension Sweep this loop (SAME for loops 1-3 of this run). This loop's investigation broadened the evidence base: `StoreNameLookup`'s three locked caches + `SteamGridDbClient.CapsuleParseNotes` (4, as before) **plus, newly confirmed this loop, `FixLog`'s three static fields** (`lines`/`fileName`/`logFolder`, `FixLog.cs:24,26,28`, read in full) — 7 process-lifetime ambient-state instances total, all exceeding the 9-anchor's "one or two ambient-context dependencies" allowance. Per the Delta Derivation Guardrail, finding more instances of an already-below-anchor defect does not itself move the score further down — the qualitative picture (well past "one or two") is unchanged, so this stays SAME rather than reflexively DOWN. Named candidate: consolidating these behind fewer owners (DI-style injection) was considered and rejected this loop — it fails Simplify Pressure Test Q2 (not the smallest honest fix; this app is a single-instance Game Bar widget with no multi-instance or test-injection need beyond the settable `RecordFolder`/`LogFolder` pattern already used for `ApplicationData.Current`, and threading cache/log state through the many call chains that reach these fields would be a much larger redesign than any single loop's blast radius should attempt). This is the source-backed blocker satisfying the Stalled-Dimension Sweep for this dimension.
- Framework / platform best practices: **9.5** | SAME | `residual_disposition: accepted` | `App.xaml.cs:120`'s `//TODO: Load state from previously suspended application` re-read in full this loop, unchanged. **Adversarial Pass re-run**: deleting the dead comment outright still passes SPT Q1-Q4 as a free, zero-risk subtractive change but fails Q5 comparatively — smaller gain than this loop's actual pick, and bundling it would cross into a fourth unrelated file, breaking F-016's own bounded blast radius. SPT-rejected on Q5; residual holds, accepted.
- Concurrency and runtime safety: **7.0** | UP | Scored against source *as read at Step 1* (after this loop's own Step 2/3 edit is written, per the project's established convention that F-015-class fixes are credited the loop **after** they land — see Builder Notes). Structural proof for the UP: `git log <loop-3-parent>..d062d1d` shows loop 3's own commit (three dedicated `SemaphoreSlim` gates added to `StoreNameLookup`'s `gogNameCache`/`epicNameCache`/`nameMatchCache`), independently re-verified this loop by a full direct re-read of `StoreNameLookup.cs` (all 361 lines) — `gogNameGate`/`epicNameGate`/`nameMatchGate` all present, double-checked-locking pattern correct, matches `AsyncLazyCache<T>`'s already-proven shape. Tempering the UP: this loop's own investigation found the identical defect class in a second location, not yet fixed — `SteamGridDbClient.NoteCapsuleParse` (`SteamGridDbClient.cs:49-55`) performs an unsynchronized `Count`-check-then-`Add` on the same static `CapsuleParseNotes` list (Finding F-017, new, Noticeable, queued Priority 2) — so the credit for closing one instance of this defect class is real but incomplete; F-011 remains blocked by the standing user constraint, re-derived fresh this loop (a pure extraction would not be blocked, but F-011's own remedy — bounded concurrency — necessarily changes network-call ordering, so it stays genuinely blocked).
- Code simplicity and clarity: **9.5** | SAME | `residual_disposition: accepted` | Mandatory leaf-module duplication sweep this loop (three parts per method.md Step 6): (a) leaf modules read — full direct read of `PrimaryWidget.xaml.cs` (2067 lines pre-fix), `StoreNameLookup.cs` (361 lines), `SteamGridDbClient.cs` (413 lines), `AppliedArtworkStore.cs`, `FixLog.cs`, `GameEntry.cs`, `GamePlatform.cs`, plus two independently-briefed helper sweeps covering `Services/Artwork/*`, `Services/Library/*`, `Services/SteamGridDB/*`, `Services/Stores/*`, `Models/*`, `App.xaml.cs`, `MainPage.xaml.cs`; (b) four-angle results — Reuse: clean (F-002/F-003/F-013's `SlidePanelAsync`/`ConfirmAndRunAsync`/`UpdateSharedEntriesAsync` all confirmed still the sole call path at their respective sites, no drift); Simplification: the `gridPanelSessionId`/`searchPanelSessionId` session-token pattern (increment-before-await, capture-then-compare-after-await, duplicated across ~9 call sites in `PrimaryWidget.xaml.cs`) is a genuine structural echo of F-002/F-003's own shape, but at exactly 2 instances — below this project's own established 3-instance extraction threshold (the same bar that correctly excluded `GameEntry`/`GridImageItem`'s shared `INotifyPropertyChanged` boilerplate at loop 3) — correctly not a finding, watched for a future third instance; Altitude: clean; Efficiency (D1-D4): clean, no new candidate; (c) no `audit_clones.py`/`audit-enum-interpretation.sh` available in this repo checkout — manual four-angle pass substituted, noted as a scope limit. Residual blocking 10 unchanged: `GamePlatform.cs`'s two independent switch statements (Finding F-012, Cosmetic, re-confirmed unchanged via direct read). **Adversarial Pass re-run**: F-012's smallest fix still passes SPT Q1-Q4 but fails Q5 comparatively against F-016 (this loop's actual, higher-severity, dual-dimension pick); residual holds, accepted.
- Test strategy and regression resistance: **6.5** | SAME | Scored against source *as read at Step 1*, before this loop's own Step 2/3 fix (matching the established convention). `PrimaryWidget.xaml.cs` carried zero direct or indirect test coverage as of Step 1 — the same primary-flow gap named on every loop of this project's history (13 consecutive loops, the longest stall on this scorecard; triggers the Stalled-Dimension Sweep). Mutation-test mental model re-applied: a mutation flipping `TryBeginLibraryOperation`'s `if (isLibraryOperationRunning)` to `if (!isLibraryOperationRunning)` would silently let a second write-flow start while one is already running, and no test in the pre-fix repo would catch it. **This is the named candidate satisfying the Stalled-Dimension Sweep — and, uniquely among the 13 prior citations of this same gap, this loop's own Step 2/3 (below) directly fixes it** by extracting the guard's state-transition rule into a plain, WinUI-free class (`LibraryOperationGuard`) and adding direct unit tests asserting exactly this mutation. Per the established Step-1-scores-before-Step-3 convention, credit lands at loop 5's Step 1. `Services/*` remains comprehensively covered per direct spot-checks and helper sweep; no new off-primary-flow gap found this loop beyond the previously-accepted-as-non-disqualifying set (`GameEntry.cs`, `GridImageItem.cs`, `ArtworkSource.cs`, `EpicLibrary.cs`).
- Overall implementation credibility: **9.5** | SAME | `residual_disposition: queued` (F-016) | Scored against source as read at Step 1 (pre-fix): F-016's untested-guard gap is still present in the source this Step 1 evaluates, so the residual stays `queued` rather than moving to `accepted` — it is this loop's own Priority-1 pick, and the queued-to-resolved transition (like F-013's, F-014's and F-015's before it) will show up in the *next* loop's Step 1, scored against this loop's own commit.

## Strengths That Matter
- `ArtworkRanker`/`ArtworkDownloader`/`ArtworkSignature`/`TileImage` remain a genuinely deep, pure, well-tested pipeline — independently re-confirmed this loop by a cold helper sweep finding no structural issues, no domain-policy leakage, documentation matching implementation, and static-compiled regex/one-time histogram computation with no re-derivation on match calls.
- `StoreNameLookup`'s three newly-locked caches (`gogNameGate`/`epicNameGate`/`nameMatchGate`, landed loop 3) are independently re-verified this loop as correct double-checked locking, matching `AsyncLazyCache<T>`'s already-proven shape — confirmed via both a direct full read and an independent helper sweep that explicitly checked the locking was "correct and consistent, not just present."
- This loop's own fix (F-016) required zero new architecture: `LibraryOperationGuard` is a plain `internal sealed class` with two methods and one field, placed in `Services/Library/` next to `OperationReport` — the exact precedent this project already established for extracting a Compute concern out of the untestable `PrimaryWidget.xaml.cs` (`TESTING.md`'s own documented split: "What they *compute*... is extracted and covered. What they *do* to the UI is not.").

## Findings

### Finding #1: PrimaryWidget's library-operation guard has no automated test, and the mutation-test gap it represents has been named on every loop of this project's history

**Why it matters** — `isLibraryOperationRunning`'s try-begin/end rule is the single mechanism preventing a bulk library reload and a single-game write (or two single-game writes) from racing each other; F-014 (loop 1) proved the rule's *coverage* was once incomplete, and nothing in this repository can mechanically re-prove it stays complete as the file continues to change. `test_strategy` has scored exactly 6.5 with `delta: SAME` on all 13 loops of this project's recorded history (10 pre-reset + 3 post-reset) citing this exact gap, and `architecture_quality` has been flat at 7.5 for 9 of those — the longest double stall on this scorecard.

**What is wrong** — `IsLibraryOperationBlocking`/`TryBeginLibraryOperation`/`EndLibraryOperation` (pre-fix `PrimaryWidget.xaml.cs`:194-232) implement the mutual-exclusion rule directly against a private `bool isLibraryOperationRunning` field, inline in a WinUI `Page` class. Per `TESTING.md`, `PrimaryWidget.xaml.cs` binds to `Windows.UI.Xaml`, which has no desktop projection in this repo's xunit infrastructure — so this rule could never be tested in place, unlike every other "compute" concern in this codebase (`GameImages`, `OperationReport`, `ManifestEntryIdentity`), which `TESTING.md` documents as already extracted and covered for exactly this reason.

**Evidence** — `PrimaryWidget.xaml.cs`:66 (field, pre-fix), `PrimaryWidget.xaml.cs`:194-232 (the three wrapper methods, pre-fix), `TESTING.md`:49-56 ("What is not covered... `PrimaryWidget.xaml.cs`... What they *compute*... is extracted and covered. What they *do* to the UI is not."), `SteamGridDB.Xbox.Tests/OperationReportTests.cs` + `SteamGridDB.Xbox/Services/Library/OperationReport.cs` (the precedent this fix follows).

**Architectural test failed** — Shallow module. Before the fix, the guard's Interface (the three wrapper methods) *was* its Implementation — the mutual-exclusion rule had no separate, testable form; Interface ≈ Implementation for this concern specifically, even though the surrounding class is large.

**Dependency category** — `in-process`.

**Leverage impact** — Before the fix, no caller — human reviewer or future refactor — could get a machine-checked guarantee that the mutual-exclusion rule still holds; confidence depended entirely on reading three methods together and trusting they agree, the same failure mode named at F-001/F-005 through F-009/F-014 (all previously-shipped defects in this exact file's session/guard logic).

**Locality impact** — Contained: the rule's *decision* logic (may an operation begin; is one running) moves to one 20-line class with no dependents beyond `PrimaryWidget`; the rule's *UI side effects* (status text, header button state) stay in `PrimaryWidget`, matching the split `TESTING.md` already uses.

**Metric signal** — none.

**Why this weakens submission** — Not a live bug (F-014's fix is independently re-confirmed holding this loop), but the single highest-value untested surface in the codebase by the severity anchor's own language ("test absence around central mutable runtime behavior with realistic regression risk... e.g.... cancellation logic" — this guard is exactly that class), and the only scorecard gap that has survived unchanged through every one of this project's 13 loops without ever having an actionable, unblocked fix proposed.

**Severity** — Serious deduction.

**ADR conflicts** — none.

**Minimal correction path** — Extract the guard's begin/end/is-running state machine into a new `internal sealed class LibraryOperationGuard` in `Services/Library/` (no WinUI dependency: one `bool` field, `TryBegin()`, `End()`, `IsRunning`), and have `PrimaryWidget`'s three wrapper methods delegate to it while keeping their own UI side effects (`StatusText.Text`, `SetHeaderButtonsEnabled`) unchanged. Add `LibraryOperationGuardTests.cs` asserting the exact mutation named above. No new Seam, no interface/port — a plain class, directly testable, following the `OperationReport.cs` precedent already established in this codebase.

**Blast radius** — Change: `PrimaryWidget.xaml.cs` (delegate only, no behavior change), `Services/Library/LibraryOperationGuard.cs` (new), `SteamGridDB.Xbox.csproj` (register the new file — the app project cannot glob, per `TESTING.md`), `SteamGridDB.Xbox.Tests/LibraryOperationGuardTests.cs` (new). Avoid: `StoreNameLookup.cs`, `SteamGridDbClient.cs`, `GamePlatform.cs`, `EpicLibrary.cs` (unrelated to this fix; F-017's own remedy lives in the first two, not touched this loop).

---

### Finding #2: LoadGameEntriesAsync resolves each unmatched game's name and SteamGridDB match sequentially, one network round trip at a time, on every widget open

**Why it matters** — On a library with many unmatched games, the fully-sequential per-entry network chain adds latency that scales linearly with library size.

**What is wrong** — `LoadGameEntriesAsync`'s per-entry loop (`PrimaryWidget.xaml.cs`:455-717, pre-fix line numbers, unaffected by this loop's own edit which is confined to the guard fields/methods above this range) awaits each entry's SteamGridDB platform-ID lookup, store name-fetch and SteamGridDB name search in strict sequence, even though the awaits are independent across entries.

**Evidence** — `PrimaryWidget.xaml.cs`:455-717 (pre-fix line numbers).

**Architectural test failed** — n/a.

**Leverage impact** — None currently actionable — see blocker.

**Locality impact** — Would be contained to `PrimaryWidget.xaml.cs` if unblocked.

**Metric signal** — none.

**Why this weakens submission** — A real, linearly-scaling latency cost, but **BLOCKED**: the recorded standing user constraint (no behavioural oracle for per-game network-call ordering/concurrency changes against SteamGridDB/GOG/Epic/Ubisoft) means this loop cannot land a change altering observable call ordering/concurrency/count against those services without a product decision this loop cannot make. Re-derived fresh this loop rather than just cited: a pure extraction (no ordering/count/payload change) would not be blocked, but F-011's own remedy (bounded concurrency) necessarily changes network-call ordering, so it stays genuinely blocked.

**Severity** — Noticeable weakness.

**ADR conflicts** — none.

**Minimal correction path** — BLOCKED this loop; named for continuity (Backlog Prioritization Pass criterion 0) rather than escalated to a user-decision halt, since F-016 filled this loop's actionable Priority-1 slot.

**Blast radius** — Change: none this loop. Avoid: `PrimaryWidget.xaml.cs` (no change while blocked).

---

### Finding #3: SteamGridDbClient.NoteCapsuleParse and FixLog perform unsynchronized check-then-populate/append writes on static mutable collections, the same defect class F-015 just fixed in StoreNameLookup

**Why it matters** — Both are safe today only by the same "single-threaded-per-load convention" that made `StoreNameLookup`'s three caches safe before F-015 fixed them last loop: `LoadGameEntriesAsync`'s per-entry loop awaits one entry at a time, and `FixLog`'s two callers (`LoadGameEntriesAsync`, `FixLibraryAsync`) are mutually exclusive under `isLibraryOperationRunning`. Neither guarantee is enforced by the fields themselves — a future concurrent caller (including a future unblocked F-011) would silently inherit an unsynchronized write, exactly the risk F-015's own fix eliminated for `StoreNameLookup`.

**What is wrong** — `SteamGridDbClient.NoteCapsuleParse` (`SteamGridDbClient.cs`:49-55) checks `CapsuleParseNotes.Count < 5` then calls `CapsuleParseNotes.Add(note)` with no lock — a TOCTOU race on a `List<string>`, which is not thread-safe for concurrent `Add` calls even individually (concurrent writers can corrupt the internal array, not just violate the "at most 5" invariant). `FixLog`'s three static fields (`lines`/`fileName`/`logFolder`, `FixLog.cs`:24,26,28) are mutated by `Start()`/`Write()`/read by `SaveAsync()` with no synchronization at all.

**Evidence** — `SteamGridDbClient.cs`:47 (field), `SteamGridDbClient.cs`:49-55 (`NoteCapsuleParse`), `FixLog.cs`:24,26,28 (fields), `FixLog.cs`:46-59 (`Start`/`Write`), `AsyncLazyCache.cs`:19-60 (the pattern already used to fix the identical shape in `StoreNameLookup` last loop).

**Architectural test failed** — n/a (concurrency-primitive inconsistency, matching F-015's own classification).

**Dependency category** — `in-process`.

**Leverage impact** — A caller gets no concurrent-access-safety guarantee from either type's Interface alone; safety depends entirely on today's callers happening to be sequential.

**Locality impact** — `SteamGridDbClient.cs`'s fix is fully contained to that file; `FixLog.cs`'s fix is fully contained to that file — two independent, small remedies, not one shared abstraction.

**Metric signal** — none.

**Why this weakens submission** — Not currently reachable as a live race (confirmed: `LoadGameEntriesAsync`'s loop awaits one entry at a time; `FixLog`'s two callers are mutually exclusive under the library-operation guard), but the identical structural inconsistency F-015 closed in `StoreNameLookup` last loop, found in two more locations this loop.

**Severity** — Noticeable weakness.

**ADR conflicts** — none.

**Minimal correction path** — `SteamGridDbClient`: wrap `NoteCapsuleParse`'s check-then-populate body in a dedicated `SemaphoreSlim` gate, matching `StoreNameLookup`'s per-cache gate pattern (F-015's own remedy). `FixLog`: same pattern for `Start`/`Write`/`SaveAsync`, or fold the three fields into one small owned type analogous to `OperationReport` (a `FixLogSession` holding its own lines/filename with `Start`/`Write`/`SaveAsync` as instance methods) — either resolves the atomicity gap; the latter would also collapse three loose static fields into one owned Module, worth weighing against Simplify Pressure Test Q2 when this is picked up.

**Blast radius** — Change: `SteamGridDbClient.cs`, `FixLog.cs` (independently — not the same commit unless bundled deliberately). Avoid: `PrimaryWidget.xaml.cs`, `StoreNameLookup.cs` (unrelated, F-015 already resolved).

---

### Finding #4: GamePlatformHelper's Xbox-folder-name and SteamGridDB-API-string mappings are two independent switch statements over GamePlatform with no shared source of truth

**Why it matters** — Adding or renaming a platform requires remembering to update both switches; nothing fails to compile if one is forgotten (both silently default to `Unknown`/`null`).

**What is wrong** — `FromXboxDirectory` (`GamePlatform.cs`:22-46) and `GamePlatformToSGDBApiString` (`GamePlatform.cs`:48-67) independently switch over the same platform cases with no shared table; `FromXboxDirectory` additionally handles legacy folder-name aliases (`"ubi"`/`"ubisoft"`, `"bnet"`/`"battlenet"`) with no analogue in the reverse mapping. Re-confirmed unchanged via direct full read this loop.

**Evidence** — `GamePlatform.cs`:22-46, `GamePlatform.cs`:48-67.

**Architectural test failed** — n/a.

**Leverage impact** — Each new platform pays this asserted-twice tax; small today, compounds at scale.

**Locality impact** — Contained to `GamePlatform.cs`.

**Metric signal** — none.

**Why this weakens submission** — Minor; the enum's own home module holds two independent interpretations of itself with no compiler-enforced link. This loop's Adversarial Pass re-tested the smallest fix (fold both mappings into one table) and confirmed it still fails Simplify Pressure Test Q5 against F-016.

**Severity** — Cosmetic for contest.

**ADR conflicts** — none.

**Minimal correction path** — Fold both mappings into one table keyed by `GamePlatform`, with a separate small alias list for `FromXboxDirectory`'s legacy folder names.

**Blast radius** — Change: `GamePlatform.cs`. Avoid: `PrimaryWidget.xaml.cs` (call sites unchanged).

## Simplification Check
- Structurally necessary: Extracting `isLibraryOperationRunning`'s begin/end/is-running rule into `LibraryOperationGuard` — passes the Shallow module test (the rule's Interface and Implementation were the same thing, inline, in an untestable class; now the rule has Depth behind a 3-method Interface with its own tests).
- New seam justified: false — no new Interface/port/adapter pair is introduced; `LibraryOperationGuard` is a plain concrete class, tested directly (not through a fake), following the `OperationReport`/`GameImages`/`ManifestEntryIdentity` precedent already established in this codebase for the same Compute/Do split `TESTING.md` documents.
- Helpful simplification: Also removes credibility's own queued residual (F-016) and directly targets the one gap `test_strategy` has cited on every loop of this project's 13-loop history.
- Should NOT be done: Extracting `gridPanelSessionId`/`searchPanelSessionId`'s session-token pattern in the same commit — a real structural echo of the same shape, but at exactly 2 instances it sits below this project's own established 3-instance extraction threshold; bundling it would widen this loop's blast radius past one concern (fails Simplify Pressure Test Q2, not the smallest honest fix for *this* finding). Fixing F-017 (`CapsuleParseNotes`/`FixLog`) in the same commit — a different file, different defect class (atomicity vs. testability), would cross this loop's own bounded blast radius for no shared mechanism.
- Tests after fix: No existing tests were deleted (none existed for this surface — `PrimaryWidget.xaml.cs` carried zero test coverage before this loop, per `TESTING.md`'s own documented boundary). Five new tests added directly at the new Interface (`LibraryOperationGuardTests.cs`): starts-not-running, `TryBegin` succeeds when free, `TryBegin` fails (and leaves state unchanged) when already running — the exact mutation this finding names — `End` releases the guard, `End` is safe to call when idle. Verification: full build (msbuild, exit 0) and full test suite (run-tests.ps1, 143 passed / 0 failed / 0 skipped — 138 prior + 5 new) both re-run before and after the change; a line-by-line diff review confirms `PrimaryWidget`'s three wrapper methods produce byte-identical `StatusText`/`SetHeaderButtonsEnabled` side effects and return values to the pre-fix version.

## Improvement Backlog
1. **[F-016]** Extract PrimaryWidget's library-operation guard into a directly-testable class (Finding F-016) — structural, needed for winning. Breaks the 13-loop `test_strategy` stall and the 9-loop `architecture_quality` stall simultaneously. Score impact: `architecture_quality +0.5; test_strategy +0.5`.
2. **[F-017]** Wrap `SteamGridDbClient.CapsuleParseNotes`'s check-then-populate write and `FixLog`'s static fields in synchronization, matching F-015's own remedy pattern (Finding F-017) — structural, helpful. Score impact: `concurrency +0.5`.
3. **[F-011]** Parallelize `LoadGameEntriesAsync`'s per-entry network resolution (Finding F-011) — efficiency, needed for winning once unblocked. **BLOCKED** by the standing user constraint on per-game network-call ordering/concurrency; named for continuity, not actionable this loop. Score impact: `concurrency +1.0`.

**Priority-1 accounting**: The Stalled-Dimension Sweep (mandatory from loop 4 on) triggered on `architecture_quality` (SAME for all 3 loops of this run, 9 of the last 10 overall) and `test_strategy` (SAME for all 13 loops of this project's recorded history — the longest stall on this scorecard) and `data_flow` (SAME for all 3 loops of this run; disposed of via a named, SPT-Q2-rejected candidate rather than a fix, since consolidating ambient state would be a much larger redesign than any single loop's blast radius). F-016 is the only candidate this loop that directly breaks a stall — and it breaks two at once — while following an established, low-risk, already-precedented extraction pattern (`TESTING.md`'s own documented Compute/Do split). F-017 ranks below it: real and unblocked, but touches only `concurrency`, which is not itself stalled this loop (already moving UP via F-015's credit). F-011 stays blocked (criterion 0).

## Deepening Candidates
None formally listed this loop (0-3 required; each must cite a Finding ID as friction proof per G10, and the two standing candidates below have not yet accumulated that proof). Two watched patterns, neither yet a Finding:
- `LoadGameEntriesAsync`'s manifest-parsing/game-identity-resolution block (`PrimaryWidget.xaml.cs`:388-758 pre-fix) remains the standing candidate Module extraction, re-examined independently this loop and reaching the same conclusion as loops 1-3: `CreateThumbnailAsync`'s `Dispatcher`-thread affinity and `GameEntry`'s WinRT-typed properties (`BitmapImage`, `StorageFolder`) mean a clean extraction requires first splitting `GameEntry` into a pure DTO plus a UI-bound wrapper — a larger, simultaneous redesign, not a smallest honest fix.
- The `gridPanelSessionId`/`searchPanelSessionId` session-token pattern (see Simplification Check) sits at exactly 2 duplicated instances — below this project's own established 3-instance bar. If a third instance of the same increment-before-await/capture-then-compare-after-await shape appears in a future loop, it becomes a legitimate F-002/F-003/F-013-class finding; watched, not acted on.

## Builder Notes

**Credit for a fix lands at the START of the next loop's Step 1, not inside the loop that made the fix.** This loop's own scorecard shows both halves of the pattern again: `concurrency` moved up crediting loop 3's already-landed F-015 fix, while `architecture_quality`, `test_strategy` and `credibility` stayed flat even though this loop's own Step 2/3 is about to fix the exact thing keeping two of them capped — because Step 1 scores the source as it exists before that fix runs. This is the fourth loop running this exact pattern (F-013 at loop 2, F-014 at loop 1, F-015 at loop 3, now F-016 at loop 4) — consistent enough across independently-derived loops to be a load-bearing project convention, not a one-off coincidence.

**The same untested-surface citation, repeated for 13 loops, was never itself investigated as a fixable architecture problem until this loop asked "why can't this specific piece be tested" instead of "this can't be tested."** Every prior loop correctly named `PrimaryWidget.xaml.cs`'s guard logic as the primary-flow mutation gap capping `test_strategy`, and every prior loop correctly cited `TESTING.md`'s platform-binding explanation for why the *file* has no desktop projection. What went unexamined for 13 loops is that the specific *rule* being tested (a boolean mutual-exclusion guard) has zero dependency on `Windows.UI.Xaml` itself — only its UI side effects do. The file being untestable does not mean every piece of logic inside it is.

**A duplication pattern below the project's own established instance threshold is a candidate to watch, not a finding to force.** This loop found the `gridPanelSessionId`/`searchPanelSessionId` session-token idiom duplicated at exactly 2 sites — structurally the same shape as F-002/F-003 (fixed at loops 7-8) — but this project has consistently (loop 3's `GameEntry`/`GridImageItem` `INotifyPropertyChanged` boilerplate call) treated 2 instances as below the bar that justifies extraction, reserving that for 3+. Applying the same bar here rather than lowering it for a pattern that "looks similar to a fixed one" avoids inventing ceremony a future third instance may never arrive to justify.

**Scorecard humility check.** Three claims this loop's critic is least confident about: (1) F-016's severity as "Serious deduction" rather than "Likely disqualifier" — the severity anchor's own language ("test absence around central mutable runtime behavior with realistic regression risk... e.g.... cancellation logic") describes this guard almost verbatim, and a stricter reading could argue 13 consecutive loops of an unfixed primary-flow test gap on the app's single most load-bearing concurrency-safety mechanism crosses into disqualifying territory regardless of the fact that nothing is *currently* broken; this loop judged "no live-reachable harm" as the deciding factor between the two anchors, which is a real distinction but not an airtight one. (2) `concurrency`'s UP move to 7.0 rather than a smaller or larger step — crediting F-015's real fix while discovering F-017's real new instance in the same investigation makes the net direction genuinely ambiguous, and a stricter reading could hold the score at 6.5 (net zero: one closed, one opened) rather than net-crediting a small UP; this loop judged that F-015's fix is a completed, verified structural change while F-017 is merely a newly-*found*, not yet *worsening*, pre-existing condition, which tips the balance UP but not by much. (3) `data_flow` held at 7.5 rather than nudged down for finding 3 *more* ambient-state instances than previously counted (4 → 7) — the Delta Derivation Guardrail explicitly warns against mechanical down-scoring on "found a new instance of a known defect class" without independent re-derivation, and this loop's own reasoning (the qualitative picture was already "well past one or two" and stays so) is a judgment call about what counts as *materially* new evidence versus merely *more* evidence of the same shape, which a stricter reading could contest either way.

## Final Judge Narrative
Place, not win, yet — but this loop breaks new ground rather than repeating the pattern of the last three. Concurrency is credited for a real, verified fix (F-015, landed loop 3) while this loop's own investigation immediately found the same defect class recurring in a second location (F-017, new, queued) — a reminder that closing one instance of a defect class does not close the class. The larger event this loop is F-016: for the first time in this project's 13-loop history, the `test_strategy` gap that every prior loop named and declined to act on (PrimaryWidget's untested primary-flow guard logic) has an actionable, unblocked, low-risk fix, and this loop takes it — not by inventing new architecture, but by applying a pattern this codebase already uses everywhere else (`TESTING.md`'s documented Compute/Do split). Runtime ownership remains trustworthy for every traced write path (F-014 holds, independently re-verified). Simplification did not hurt this loop: zero new ceremony, zero new Seams, a plain class following an established precedent. Tests newly cover a primary-flow concurrency-safety mechanism that no test in this repository could previously reach at all — a first, not an increment. Future work still risks over-engineering if it tries to extract PrimaryWidget's orchestration wholesale, unify StoreNameLookup's differently-shaped caches behind one generic abstraction, or extract the session-token pattern before a third instance justifies it; this loop re-examined all three candidates independently and reached the same restrained conclusions prior loops did, for source-backed reasons rather than by anchoring to the prior verdict.

## Loop 4 Result
Extracted `PrimaryWidget`'s library-operation guard (`isLibraryOperationRunning` plus the begin/end/is-running rule in `IsLibraryOperationBlocking`/`TryBeginLibraryOperation`/`EndLibraryOperation`) into a new `internal sealed class LibraryOperationGuard` (`SteamGridDB.Xbox/Services/Library/LibraryOperationGuard.cs`) with a `bool IsRunning`, `bool TryBegin()` and `void End()` — no WinUI dependency, following the same Compute/Do split `TESTING.md` documents for `GameImages`/`OperationReport`/`ManifestEntryIdentity`. `PrimaryWidget.xaml.cs`'s three wrapper methods now delegate their state-transition decision to a `private readonly LibraryOperationGuard libraryOperationGuard` field while keeping their own UI side effects (`StatusText.Text = busyStatusText`, `SetHeaderButtonsEnabled(...)`) exactly where and when they ran before — a line-by-line diff review confirms both methods' bodies are behavior-preserving. Registered the new file in `SteamGridDB.Xbox.csproj` (the app project cannot glob per `TESTING.md`). Added `SteamGridDB.Xbox.Tests/LibraryOperationGuardTests.cs` with five tests, including one asserting the exact mutation this finding names (`TryBegin` returning `false` and leaving the guard `IsRunning` when already running). Full build (msbuild, exit 0) and full test suite (run-tests.ps1) both re-run before and after the change: before, 138 passed / 0 failed / 0 skipped; after, 143 passed / 0 failed / 0 skipped (138 prior + 5 new, all green, no prior test's behavior changed). No network call, UI side effect, or return value changed anywhere. Finding F-016 (stable_id F-016) is **resolved**, pending the Implementation Review Pass. No unintended scorecard regression observed; F-017 (CapsuleParseNotes/FixLog) was discovered as a fresh finding during this loop's own Step 1 investigation, not introduced by this loop's edit (neither `SteamGridDbClient.cs` nor `FixLog.cs` was touched).
